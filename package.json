{
  "name": "steam-ignore-like-a-pro",
  "version": "1.1.0",
  "description": "Helper tool for ignoring games in the Steam Store.",
  "author": "",
  "license": "MPL-2.0",
  "scripts": {
    "build": "node build.js",
    "build:test": "node build.js --test",
    "test": "npm run build:test && npx playwright test --project=chromium",
    "test:auth": "npm run build:test && npx playwright test --project=setup",
    "test_dq": "npm run build:test && npx playwright test tests/discovery-queue",
    "test_eq": "npm run build:test && npx playwright test tests/explore-queue",
    "test_mi": "npm run build:test && npx playwright test tests/manual-ignore",
    "test_popup": "npm run build:test && npx playwright test tests/popup",
    "test_curator": "npm run build:test && npx playwright test tests/curator"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0"
  }
}