{
  "name": "steam-ignore-like-a-pro",
  "private": true,
  "description": "Helper tool for ignoring games in the Steam Store.",
  "author": "",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "build": "node build.js",
    "build:test": "node build.js --test",
    "test": "npm run build && npm run build:test && npx playwright test --project=chromium",
    "test:ff": "npm run build:test && npx playwright test --project=firefox",
    "test:canary": "npx playwright test --project=canary",
    "test:units": "npx playwright test --project=chromium unit.spec.js tests/cross-cutting tests/explore-queue/decision-matrix.spec.js",
    "test:auth": "npm run build:test && npx playwright test --project=setup",
    "test_dq": "npm run build:test && npx playwright test --project=chromium tests/discovery-queue",
    "test_eq": "npm run build:test && npx playwright test --project=chromium tests/explore-queue",
    "test_mi": "npm run build:test && npx playwright test --project=chromium tests/manual-ignore",
    "test_popup": "npm run build:test && npx playwright test --project=chromium tests/popup",
    "test_curator": "npm run build:test && npx playwright test --project=chromium tests/curator",
    "test_live": "npm run build:test && npx playwright test --project=chromium tests/curator/drain-live.spec.js",
    "test_sw_live": "npm run build && npx playwright test --project=chromium tests/curator/sw-live.spec.js",
    "test_widget": "npm run build:test && npx playwright test --project=chromium tests/widget"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0"
  }
}